import cmath
n=complex(input())
print(abs(n))
print(cmath.phase(n))
#complex function converts the input of complex no. for i.e. is a+ib into parameterised form which can be understood by the phase and abs function.
